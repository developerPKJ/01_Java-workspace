//패키지 선언부 - 이 클래스를 생성할 때 선택한 패키지의 위치를 나타내는 구문
package com.kh.first;

import java.util.Scanner;

import com.sun.jdi.request.MethodExitRequest;

import jdk.internal.org.jline.utils.ShutdownHooks.Task;

// 클래스 명 : com.kh.first.HelloEveryone
public class HelloEveryone {

	/* 
	 * 사칙연산
	 * 코드
	 */
	
	/*
	 * 자바 프로그래밍 순서
	 * 1. workspace 생성
	 * 2. project 생성
	 * 3. package 생성
	 * 4. class 생성
	 * 5. 코드 작성
	 */
	
	/*
	 * 패키지 내에 들어가는 클래스 이름 간의 중복은 기존의 폴더 규칙과 동일함
	 * 기본 패키지(src)에 클래스를 바로 넣는 것은 권장하지 않음 --> 추후 확장성과 유지보수성
	 */
	
	/*
	 * 패키지명은 모두 소문자 --> 소문자로 시작해야 풀 클래스명을 봤을 때 어디까지 패키지고 어디부터 클래스인지 한눈에 확인 가능하기 때문(com.kh.first.HelloEveryone)
	 * 패키지는 적어도 세 단계 이상으로 만들기
	 * 패키지명의 두 번째 레벨까지는 도메인 주소의 역순으로
	 */
	
	/*
	 * 메소드 - 사전적으로 기능 의미(하나의 기능 단위)
	 * 한 개의 클래스 안에 여러 개의 메소드를 작성할 수 있음
	 * 메소드 이름도 중복되면 안되지만 가능한 경우가 있음(아마 오버로딩? 오버라이팅? 말하는거 아닐까)
	 * 클래스 안에 메소드를 작성하고 그 안에 소스코드를 작성
	 * 이렇게 작성한 메소드 안의 코드를 실행하려면 호출
	 * 
	 * 메소드 종류
	 * 1. 메인 메소드 : 프로그램의 시작점(entry point)을 나타내는 부분
	 * - 프로그램이 실행되는 순간 제일 먼저 실행되는 메소드
	 * - 한 프로그램 당 적어도 1개 이상은 꼭 갖고 있어야함
	 * 2. 일반적인 메소드
	 * - 메인 메소드를 제외한 나머지 모든 메소드들
	 * - 다른 메소드에서 호출을 통해서만 실행 가능
	 */
	
	static int returnType(String b) {
		int type;
		
		if (b.equals("+")) {
			type = 0;
		}else if (b.equals("+")) {
			type = 1;
		}else if (b.equals("*")) {
			type = 2;
		}else if (b.equals("/")) {
			type = 3;
		}else {
			System.exit(0);			
		}
		
		return type;
	}
	
	static void calculationMethod(String a, String c, int type) {
		int x = Integer.parseInt(a);
        int y = Integer.parseInt(c);

        switch (type) {
            case 0 -> System.out.println(x + y);
            case 1 -> System.out.println(x - y);
            case 2 -> System.out.println(x * y);
            default -> System.out.println((double) x / y);
        }
	}
	
	// static : 재사용 가능
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		System.out.println("Hello, Everyone!");
		Scanner scan = new Scanner(System.in);
		
		int i = 0;
		
		while(i == 0) {			
			System.out.println("아무 사칙연산 수식 입력(a + b)");
			
			String a = scan.next();
			String b = scan.next();
			String c = scan.next();
			
			calculationMethod(a, c, returnType(b));
		}
			
		scan.close();
	}

}
