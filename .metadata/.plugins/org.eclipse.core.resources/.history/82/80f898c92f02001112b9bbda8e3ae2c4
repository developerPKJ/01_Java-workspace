package com.kh.second.run;

import com.kh.second.MethodTest;

public class Run {

	//main 메소드만 갖고있는 클래스 : 실행용 클래스
	//main 메소드는 보통 별도 실행용 클래스에 따로 분리해서 작성함
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		/*
		 * 호출 방법
		 * 1. import - 다른 패키지에 존재하는 클래스를 불러 올 때, 같은 패키지 내에 존재하는 클래스면 필요 없음
		 * 2. new 사용해서 인스턴스명 앞에 붙이기 or 호출할 함수가 static이면 클래스명 앞에 붙이기
		 * 
		 * 1번 번외 해결법 : com.kh.second.MethodTest mt = new com.kh.second.MethodTest(); --> import 사용 안함(실무에서 많이 쓰는 방식은 아님)
		 */
		MethodTest.c();					//다른 클래스의 매소드를 호출하기 위해 클래스명을 앞에 붙여줌(기본적으로 매소드 이름만 표기하는건 같은 클래스 내에 함수만 호출)
		System.out.println();
		MethodTest.b();
		
		MethodTest mt = new MethodTest();	// 인스턴스 명은 소문자로 시작하는게 약속(사실 변수명이랑 같은 개념)
		mt.d();
	}
}